[build]
  command = "node scripts/generate-static-pages.js"
  ignore = 'test "$SITE_NAME" = "adpeprod" && test -z "$INCOMING_HOOK_TITLE"'

[images]
  remote_images = ["https://adpe-architettura-galleries.s3.eu-north-1.amazonaws.com/.*"]
